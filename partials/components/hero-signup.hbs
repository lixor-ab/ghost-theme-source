{{#get "pages" filter="slug:hero-signup"}}
    {{#foreach pages}}
        <section class="gh-outer hero-signup">
            <div class="gh-inner">
                {{{html}}}
            </div>
        </section>
    {{/foreach}}
{{/get}}
